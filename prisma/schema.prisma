generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WriteForm {
  id            Int      @id @default(autoincrement())
  firstName     String
  lastName      String
  phone         String
  email         String
  streetAddress String?
  city          String?
  state         String?
  zipCode       String?
  companyName   String?
  interests     String[] // stores multiple checkbox selections
  comments      String?
  createdAt     DateTime @default(now())
}
